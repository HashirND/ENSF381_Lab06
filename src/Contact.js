import React from 'react';

const Contact = ({ title, description }) => {
  // contact comp
  return (
    <div>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
};

export default Contact;
